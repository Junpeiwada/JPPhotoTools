# macOS
.DS_Store

# Swift / SwiftPM
.build/
*.xcworkspace/xcuserdata/
DerivedData/
.swiftpm/

# xcodebuild のローカル成果物（DerivedData / 解決済み SwiftPM パッケージ）
App/build/

# XcodeGen が project.yml から生成する成果物（リポジトリには含めない）
*.xcodeproj/

# Xcode user state
xcuserdata/
*.xcuserstate

# Python
__pycache__/
*.pyc

# Tools 共通 venv（マシンごとに作成。コミットしない）
.venv/

# Node（開発補助スクリプト用 package.json。依存は無いが念のため）
node_modules/
